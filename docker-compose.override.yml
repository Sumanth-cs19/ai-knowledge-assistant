services:
  ui:
    ports:
      - "4200:80"

  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      AI__Endpoint: ${AI_ENDPOINT:-http://host.docker.internal:11434}
    ports:
      - "5000:8080"

  postgres:
    ports:
      - "5432:5432"
